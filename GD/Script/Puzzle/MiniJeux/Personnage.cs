using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Personnage : MonoBehaviour
{

    //public Vector2Int positionGrille;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    //public void Deplacement(Vector2Int direction)
    //{
    //    positionGrille += direction;
        
    //    if (positionGrille.x < 0)
    //    {
    //        positionGrille.x = 0;
    //    }

    //    if (positionGrille.y < 0)
    //    {
    //        positionGrille.y = 0;
    //    }

    //    if (positionGrille.x > FindObjectOfType<Grille>().longeur - 1)
    //    {
    //        positionGrille.x = FindObjectOfType<Grille>().longeur - 1;
    //    }

    //    if (positionGrille.y > FindObjectOfType<Grille>().largeur - 1)
    //    {
    //        positionGrille.y = FindObjectOfType<Grille>().largeur - 1;
    //    }
    //}

    private void Deplacement()
    {

    }
}
